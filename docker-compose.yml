version: "3.8"

services:
  buzzy-frontend:
    image: buzzy-frontend:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - REACT_APP_API_URL=http://localhost:3000


  # buzzy-backend:
  #   image: buzzy-backend:latest
  #   build:
  #     context: ../buzzy-backend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DATABASE_URL=postgresql://buzzy_user:buzzy_password@db:5432/buzzy_db
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:13
  #   environment:
  #     - POSTGRES_USER=buzzy_user
  #     - POSTGRES_PASSWORD=buzzy_password
  #     - POSTGRES_DB=buzzy_db
  #   volumes:
  #     - db_data:/var/lib/postgresql/data